<Window x:Class="CypherBotWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CypherBotWPF"
        mc:Ignorable="d"
        Title="CypherBot" Height="550" Width="1080">
    <Grid Background="LightGray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <!-- ══════════════ LEFT: the original chat interface (unchanged) ══════════════ -->
        <StackPanel Grid.Column="0" Margin="20">

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Image Source="robot.png" Width="50" Height="50" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="CYPHERBOT - Cybersecurity Awareness Assistant"
                           FontSize="24"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"
                           TextAlignment="Center"/>
            </StackPanel>

            <RichTextBox x:Name="txtChat"
                 Height="350"
                 Width="650"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,10"
                 Background="White"/>

            <TextBox x:Name="txtMessage"
                 Height="35"
                 Width="650"
                 Margin="0,0,0,10"/>

            <!-- Buttons placed side-by-side -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Send"
                        Width="100"
                        Height="35"
                        Click="Button_Click"/>
                <Button Content="Exit"
                        Width="100"
                        Height="35"
                        Margin="10,0,0,0"
                        Click="ExitButton_Click"/>
            </StackPanel>

        </StackPanel>

        <!-- ══════════════ RIGHT: live view of the tasks database table ══════════════ -->
        <StackPanel Grid.Column="1" Margin="0,20,20,20">

            <TextBlock Text="📋 Tasks (Database)"
                       FontSize="16"
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>

            <DataGrid x:Name="dgTasks"
                      Height="420"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Background="White"
                      RowBackground="White"
                      AlternatingRowBackground="#F0F0F0"
                      FontSize="11">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="30"/>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="100"/>
                    <DataGridTextColumn Header="Reminder" Binding="{Binding ReminderText}" Width="70"/>
                    <DataGridCheckBoxColumn Header="Done" Binding="{Binding IsCompleted}" Width="40"/>
                </DataGrid.Columns>
            </DataGrid>

            <Button Content="Refresh"
                    Height="30"
                    Margin="0,10,0,0"
                    Click="RefreshTasksButton_Click"/>

        </StackPanel>

    </Grid>
</Window>